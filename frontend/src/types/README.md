# Types Directory

TypeScript utility types and type helpers only.

Main data structures are defined as Zod schemas in `src/schemas/` and types are derived from them using `z.infer<>`.

This directory is for utility types that don't map to data structures.